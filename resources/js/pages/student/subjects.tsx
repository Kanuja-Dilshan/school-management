import {Head,Link,router} from '@inertiajs/react';
export default function Subjects({subjects,appliedSubjectIds}:any){return <><Head title="Subjects"/><div className="p-8 max-w-6xl mx-auto"><div className="flex justify-between mb-6"><div><Link href="/dashboard" className="underline">← Dashboard</Link><h1 className="text-3xl font-bold mt-3">Available Subjects</h1></div><Link href="/student/applications" className="border rounded-lg px-4 py-2 h-fit">My Applications</Link></div><div className="grid md:grid-cols-3 gap-5">{subjects.map((s:any)=>{const applied=appliedSubjectIds.includes(s.id);return <div className="bg-white rounded-2xl shadow p-5"><h2 className="text-xl font-bold">{s.name}</h2><p className="text-slate-500">{s.code}</p><p className="mt-3">{s.description}</p><p className="mt-3">Teacher: {s.teacher?.name??'Not assigned'}</p><button disabled={applied} onClick={()=>router.post(`/student/subjects/${s.id}/apply`)} className="mt-5 w-full rounded-lg bg-slate-900 text-white py-2 disabled:bg-slate-300">{applied?'Already Applied':'Apply'}</button></div>})}</div></div></> }